# ~/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022

LOCAL_PATH=$HOME/local/bin
LOCAL_LIB_PATH=$HOME/local/lib
LOCAL_LIB64_PATH=$HOME/local/lib64

[ -d $LOCAL_LIB_PATH ] && LD_LIBRARY_PATH=$LOCAL_LIB_PATH:$LD_LIBRARY_PATH
[ -d $LOCAL_LIB64_PATH ] && LD_LIBRARY_PATH=$LOCAL_LIB64_PATH:$LD_LIBRARY_PATH
[ -d $LOCAL_PATH ] && PATH=$LOCAL_PATH:$PATH

unset LOCAL_LIB_PATH LOCAL_LIB64_PATH LOCAL_PATH

export GOROOT=$HOME/go
export GOBIN=$GOROOT/bin
export GOPATH=$HOME/go/pkg
export PATH=$GOBIN:$PATH

export LD_LIBRARY_PATH PATH


# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
    fi
fi

